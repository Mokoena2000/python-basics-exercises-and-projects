#Create a program which stores a value in a variable and displays it

def access():
    if name == "moholeng":
        return  (f"hello {name} welcome back")
    
    else:
        return (f"hello {name} sorry go to own accouunt")
    
name = input("Enter your name: ")

print(access())